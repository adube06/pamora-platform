<?php

// Domain-owned API routes are registered by each domain's ServiceProvider
// under the /api/v1 prefix (see app/Domains/*/routes-api.php), per the
// API Standards. This file intentionally stays empty.
